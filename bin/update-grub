#!/usr/bin/bash

cfgfile=/boot/efi/grub/grub.cfg

# check fi run as root
if [ "$(id -u)" -ne 0 ]; then
    echo "This sciprts must run as root"
    exit 1
fi

echo "[*] Updating ..."
grub-mkconfig -o $cfgfile
echo "[*] Updating done"

